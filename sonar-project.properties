# Required metadata
sonar.projectKey=python_on_sq
sonar.projectName=Python on SC
sonar.projectVersion=1.0
sonar.organization=guillaume-dequenne-sonarsource

# Comma-separated paths to directories with sources (required)
sonar.sources=src
sonar.tests=tests

# Unit tests
sonar.python.coverage.reportPaths=cov.xml
